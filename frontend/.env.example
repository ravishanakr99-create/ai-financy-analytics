# ===========================================
# API Base URL - Eligibility Report Frontend
# ===========================================
#
# DEV (Vite proxy - recommended):
#   Leave empty or omit this file.
#   Requests go to /api/v1 (same origin), Vite proxies to backend.
#
# DEV (direct to backend - if proxy fails):
#   VITE_API_URL=http://127.0.0.1:8000/api/v1
#   Use 127.0.0.1 not localhost on Windows (avoids IPv6 issues).
#
# PRODUCTION:
#   VITE_API_URL=https://your-api.example.com/api/v1

# VITE_API_URL=
